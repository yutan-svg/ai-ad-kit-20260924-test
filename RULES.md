# 制作の決まり

このファイルが決まりの正本です。前半が**制作の流れ**（いつ・何を・どの順でやるか）、後半が**守る決まり**（個々の判断基準）です。
全文を毎回読む必要はありません。いま触っている領域（音声／レイアウト／書き出し／表現）の節だけを引いてください。

**このうち5つは、文章ではなく機械が止めます**（第1部0節）。スクリプトが拒否したら、回避せず原因を直してください。

---

# 第1部　制作の流れ

## 0. 機械で止まる決まり

守れなかったことが実際に起きた決まりは、文章ではなくスクリプトの側に移しました。
**引数で無効にできるものには、必ず理由を書かせます。** 回避策を探すのではなく、止まった理由を直してください。

| 決まり | どこで止まるか | 止まる条件 | 例外の出し方 |
|---|---|---|---|
| 人物の参照を渡す | `generate_lines.py` / `generate_google.py` | 2セリフ目以降に参照が無い（Seedance は `reference_video` の公開URL か `source_task_id`、Google は `reference_image`） | `--allow-no-reference "理由"`（台帳に残る） |
| 同じセリフは2回まで | `generate_lines.py` / `generate_google.py` / `generate_talking_head.py` | `out/takes-ledger.json` の回数＋今回が3回目以上 | `--force "理由"`（台帳に残る） |
| 生成の前に承諾を得る | `generate_lines.py` / `generate_google.py` / `generate_talking_head.py` | `out/generation-approval.json` が無い・対象や本数が違う・台本が更新された | なし（承諾を取る） |
| 危険語を耳で確かめる | `preflight.py` | 数字・固有名詞・辞書登録語を含むセリフに `out/ear-check.json` の記録が無い | なし（`ear_check.py` で聴く） |
| 書き出しの前に承諾を得る | `remotion.config.ts` / `npm run render` | `out/approval.json` が無い・その成果物ではない・`src/scenes.ts` の更新より古い | なし（承諾を取る） |
| 書き出しの前に preflight を通す | `approve.py`（承諾の記録と `--check`） | `out/preflight-ok.json` が無い・`src/scenes.ts` が変わった・`--skip-audio` で通した | なし（`preflight.py` を通す） |
| 音の出所を台帳に書く | `preflight.py` | `src/` で使う `audio/` の音が `knowledge/assets.md` の「音」の表に無い、または利用条件が空 | なし（出所を書く。合成音を使わない） |
| 聞き取りをファイルに残す | `build_research.py` / `generate_*.py` / `preflight.py` / `approve.py --check` | `knowledge/project.md` の商材・届ける相手（リサーチ前）、台本前の3つ・着地（台本・生成・書き出し前）が空欄 | なし（依頼主の答えを書く。推測で埋めない） |

承諾と耳の確認は、**対話（tty）でしか記録できません。** AI が `out/` のファイルを書いて代わりにすることは禁止です
（耳の確認は、聴いた音声の中身のハッシュが記録され、`preflight.py` が実ファイルと照合します）。

## 1. 最初に決めること

台本は依頼主が用意します（AI と相談しながら作っても構いません）。**最初に「持ち込む／一緒に作る／調べてから作る」のどれかを依頼主に選んでもらいます**（指示書「手順の入口」）。台本が決まったら、作り始める前に次の3つを確認します。

| 決めること | どこに書くか |
|---|---|
| 商材・相手・言ってよいこと／いけないこと・必須表記 | `knowledge/project.md` |
| 使う道具（AI 映像制作ツール／読み上げ／実写素材のどれで作るか） | 依頼主と相談して決める |
| どちらの座組みで作るか（**Google 完結型**＝Veo／Omni／Nano Banana／Gemini、**汎用型**＝Seedance ほか） | 依頼主と相談して決める（`guides/google-models.md`。声を指定する案件は汎用型） |
| 縦横比（9:16 か 4:5 か両方か）と尺 | `src/scenes.ts` と書き出しの指定 |

`knowledge/project.md` が空のまま制作に入ってはいけません。禁止表現と必須表記が決まっていないと、作ったものを捨てることになります。
聞き取った答えは**チャットに置いたままにせず、その場でファイルに書きます**。次のチャット・別の AI・別の担当者には、ファイルしか引き継がれません。空欄のままだとリサーチ・生成・検査・書き出しのスクリプトが止まります（`python3 scripts/check_project.py --for script` で確かめられます）。

### 台本の叩き台を書く前に、3つを会話で確かめる

台本を書き始める前に、次の3つを依頼主に確認します。**答えが揃うまで書かないでください。**
実際に、LP から「誰が画面に出るか」を推し量って決め、冒頭を一般論で書いてしまった例があります。

| 確かめること | 選択肢の例 | どこに書くか |
|---|---|---|
| ① 画面に出るのは誰か | 申し込む側の人物／提供する側の人物／人物なしのナレーション | `knowledge/project.md` §3 |
| ② 冒頭は「誰の・どんな場面」か | 見る人の状況から決める（時間帯・持ち方・音の有無まで）。一般論の悩みから始めない | `knowledge/project.md` §3 |
| ③ 最後にしてほしい行動は何か | 申し込む／試す／資料を見る／まず知ってもらうだけ。1つに絞る | `knowledge/project.md` §3 の着地 |

**人物の年代・服装・雰囲気を、LP や思い込みで決めないでください。** 依頼主に選んでもらいます。
依頼主も決めかねているなら、`ad-research`（競合の広告と視聴シーンを一次情報で調べる）を勧めます。
調べてから決めたほうが早くなります。
聞き方は自由です。決まった言い回しはありません。専門用語を使わず、選択肢を2〜3個出して選んでもらってください。

## 2. レーンは2本

初稿を作るときと、指摘に対応して納品するときでは、かける手間が違います。混ぜないでください。

### 初稿レーン（早く見せることを優先する）

1. 台本を一息の短文に割る（決まり13）
2. 費用のかかる生成の前に、**何を・何本・どのサービスで作るかを伝えて承諾を得る**（決まり23。`scripts/approve.py --generation`）
3. **1カット目だけ**生成して人物を見てもらい、採用が決まってから参照を作って残りを生成する（決まり34）
4. 各セリフ**1テイクだけ**生成する。発音の作り直しはここではしない
5. 検品は `generate_lines.py` の自動検品と `preflight.py`、そして危険語の耳の確認（`ear_check.py`）まで。2エンジンの合議・配置の審査は初稿ではやらない
6. シーンに配線し、代表 2〜3 枚の静止画で自分の目で確かめる
7. 報告の先頭に**【初稿】**と書き、**気づいている問題の一覧**を必ず添える

初稿の報告に「気づいている問題の一覧」が無ければ、それは手を抜いた報告です。完璧に見せようとしないでください。

### 納品前レーン（指摘に対応して仕上げる）

```
指摘を受け取る
 → FIXES.md に1指摘1行で記録する（同じ指摘が過去に無いか先に照合する）
 → 直す（配置を動かしたら、その場で静止画を見て周りとの関係を確かめる）
 → 機械検査を通す（下の表）
 → 動画編集ツール（Remotion）の Studio で通して見る＋変更したシーンの静止画を台帳に添える
 → 第三者の検証（自分の目だけで終わらせない。下の4節）
 → 台帳をつけて報告する
 → 承諾を得てから書き出す（決まり23）
 → 書き出した動画そのものを検証する（verify_render.py）→ 納品
```

## 3. 機械検査の一覧

コマンドはすべてこのフォルダのルートで実行します。検査の対象やモデルの場所は `ai-ad.config.json` で指定します。

| いつ | コマンド | 何を保証するか |
|---|---|---|
| 書き出し・確認の前（必ず） | `python3 scripts/preflight.py` | テロップのはみ出し・行数・強調語の不整合・縦位置、素材の尺切れと実在、音声とシーンの尺の整合、末尾無音、納品尺、**危険語の耳の確認** |
| 数字・固有名詞を含むセリフ | `python3 scripts/ear_check.py <音声> [<音声2>] --id <id>` | 対象語だけを切り出して人に聴かせ、A/B/どちらも不可 を記録する |
| 2カット目以降の人物を揃える | `python3 scripts/rehost.py <採用した1カット目>` | 無加工の原本を一時公開して参照URLを作る |
| 費用のかかる生成の前 | `python3 scripts/approve.py --generation --ids <id…> --takes <n> --service <サービス名>` | 何を・何本・どのサービスで作るかを見せて承諾を記録する（無いと生成できない） |
| 書き出しの前 | `python3 scripts/approve.py --composition <名> --out <mp4>` | 何を書き出すかを見せて承諾を記録する（無いと書き出せない） |
| 音声を差し替えた／シーンを割った／文言を変えたら | `python3 scripts/align_captions.py` | テロップとセリフの表示タイミング |
| テロップ・図・人物の位置を動かしたら | `python3 scripts/check_overlap.py <コンポジション名> <フレーム…>` | テロップと被写体の重なり（目視で「重なっていない」と言ってはいけない） |
| 1本ずつ発音を確かめる | `python3 scripts/asr_gate.py <音声>` | 2つの音声認識エンジンの一致。不一致は要個別検証の合図 |
| テイクをまとめて検品する | `python3 scripts/generate_lines.py --lines lines.json --check-only`（Google の座組みは `generate_google.py --check-only`） | 発音・余分な語の混入・尺・禁止語 |
| 指示書と手順書を直したあと | `python3 scripts/check_parity.py` | `CLAUDE.md`・`AGENTS.md`・`GEMINI.md` が同じ内容か、`.agents/` が `.claude/` の写しとして最新か |
| 生成音声を耳で聴く前 | `python3 scripts/audio_tools.py check <音声> "キーワード"` | 突発音・残響の尾・ピークの振り切れ |
| 設計原稿（Figma）がある場合 | `npx tsx scripts/figma_compare.ts --comp <名> --figma <png> --frame <n>` | 原稿との一致を**数値で**判定 |
| 書き出した後（承諾を得た後だけ） | `python3 scripts/verify_render.py <mp4> --frames` | 成果物側の音声・台本カバレッジ・画面 |
| 書き出した後（承諾を得た後だけ） | `python3 scripts/audio_tools.py scan-gates <mp4> <各シーンの尺>` | シーン境界のクリック音 |

`--help` を付ければ各スクリプトの詳しい説明が出ます。手順書は `guides/` にあります。

## 4. 第三者の検証

自分が作ったものを自分で評価すると、「だいたい合っているはず」という思い込みが必ず入ります。報告の前に、独立した目を通してください。

| エージェント | いつ呼ぶか | 見るもの |
|---|---|---|
| `layout-reviewer` | テロップ・図・人物の配置を変えたら毎回 | 変更したシーンの静止画。重なり・余白・文字の大きさ |
| `design-verifier` | 設計原稿に合わせて実装したら | 原稿と出力の数値の一致（`figma_compare.ts` の結果込み） |
| `creative-reviewer` | 一式が仕上がったとき | 表現の規約・訴求の整合・必須表記 |
| `research-verifier` | 広告のリサーチ（`ad-research`）の資料を作ったとき | 競合の候補1社ごとに「実際に広告を出している」根拠（検索広告の写し・透明性センター・広告ライブラリ）が付いているかを先に見て、引用された URL を自分で開き直し、主張が出典どおりか・数字が一致するか・「直接確認」が本当に開かれたものか・推論に事実の印が付いていないかを4値で判定 |

加えて、指摘への対応が終わったら、**依頼主の指摘文の原文**を別のエージェントに渡し、1件ずつ解決済みかを判定させてください。要約した台帳ではなく原文を渡します。要約の時点で、実装者の解釈違いが隠れてしまうためです。

## 5. 承諾を取る場面

次の3つは、必ず先に伝えて、はっきりした承諾を得てから実行します。言い方は決まっていません。自然な言葉で構いません。

| 場面 | 伝えること |
|---|---|
| **費用のかかる生成**（AI 映像・読み上げ） | 何を（id とセリフ）・何本・どのサービスで作るか。`python3 scripts/approve.py --generation` がその内容を出して y/N を聞き、記録します |
| **書き出し**（mp4 を作る） | 何を書き出すか。`python3 scripts/approve.py --composition <名> --out <mp4>` が内容を出して y/N を聞き、記録します |
| **やり直し**（同じ箇所を作り直す） | 何が起きていて、何を変えて作り直すか |

感想なのか承諾なのか曖昧な返事だったときは、勝手に進めず聞き返してください。
一度得た承諾は、その成果物にだけ有効です。別のコンポジション・台本を変えた版・冒頭を差し替えた版は、すべて「別の成果物」です。
承諾を得たあとに `src/scenes.ts` を書き換えたら、その承諾は無効になります（`approve.py --check` が自動で落とします）。

## 6. うまくいかないときの分岐

- **音声が崩れた** → ①作り直す前に無コスト救済（頭と尾のカット、無音の詰め、音量調整）→ ②読み方の辞書と書き方を強化して作り直す（2テイクまで）→ ③2回失敗したら手法を疑う（公式ドキュメントを読む、台本を割り直す、表記を変える）
- **テロップが幅を超えた** → 文字を小さくする前に、**行の割り直し**を検討する（基準88px、最低72px）
- **図解が続いて単調** → 新しく生成する前に、素材台帳（`knowledge/assets.md`）で使い回せる映像を探す
- **同じ指摘が再び来た** → 直したつもりを疑う。修正が実際に配線・書き出しに入っていたかを、成果物側で確かめてから原因を報告する

---

# 第2部　守る決まり

## レイアウト

**1. 何にも重ねない。**
テロップと図、テロップと人物の口元、図と注釈を重ねてはいけません。人物が映るシーンではテロップは顎の下に置きます。図解のシーンでは図の下に置き、収まらない場合はテロップを動かすのではなく**図の側を作り直して**上に収めます。全面写真の上のテロップは構いません。

**1-2. 人物のシーンに図やカードを入れるなら、図はテロップの上、テロップはその下。**
図は人物の胸から画面の中央の帯（1080×1920 で概ね y=700〜1200）に置き、テロップはその下（顎の下ではなく図の下）に下げます。
画面の下端に小さなカードを置いて「図解を入れた」としてはいけません。図の中の文字は最小 44px（テロップの半分）で、
スマホで読めない図は無いのと同じです。図を入れるかどうかは、依頼主に案を見せて決めます。

**1-3. 同じ構図の人物カットを3つ以上続けない。**
正面のバストショットが続くと、内容が良くても見られません。人物が話すカットが2本続いたら、次は**状況の画**
（セリフなし・見る人の状況や場面）か、構図を変えたカット（横顔・手元・引き）を挟みます。
冒頭は「喋る人」ではなく、`knowledge/project.md` §3 で決めた「誰の・どんな場面」を画にします。
状況の画は `knowledge/assets.md` で使い回せるものを先に探し、無ければ生成の承諾を取ります。

**2. 動かしたら、生まれた余白を配分し直す。**
指示された座標だけを動かす対症療法はしないでください。要素を1つ動かせば周りとの関係が変わります。衝突は「ずらす」のではなく、レイアウトの設計をやり直して解きます。

**3. テロップの大きさは基準88px、最低72px。2行が基本で、3行を超えてはいけません。**
1行の文字数の上限は画面幅と文字の大きさで決まるので、`preflight.py` が判定します（88px なら1行10文字前後）。
図の中の文字は、いちばん小さい字でも読める大きさにします。読まれない図に存在価値はありません。

**4. テロップの色は重要度で決める。**
訴求の芯になる語だけを目立たせ、説明や接続の語は通常色にします。「新しく出る語は全部強調」はしないでください。

**5. 枠と内側の要素の余白は、上下左右を揃える。**
人は数pxのずれを気持ち悪さとして検知します。箱は padding ではなく明示的な高さを持たせ、強調枠の座標計算と一致させます。

**6. 同じ役割の図は、シーンをまたいで大きさ・位置・語彙・色を揃える。**
ばらつきは見る人に学習し直しの負担をかけます。

**7. 配置を変えたら、機械検査と `layout-reviewer` の両方に通してから報告する。**
`preflight.py` の数値検査は「重なり」を検知できません。決まり通りにするとデザインが崩れる例外的な場合は、自分で判断せず依頼主に相談してください。

**8. 図形・枠と文字のクリアランスは、画素で実測する。**
文字と枠の間は4辺とも8px以上、画面端との余白は10px以上を目安にします。同じ図が 9:16 と 4:5 の両方にある場合は、**両方で**測ります。片方だけ直して展開を忘れるのが最も多い事故です。出現アニメーションがある要素は、静止したフレームだけでなくアニメーション中のフレームも見ます（小さい状態から始まる拡大は食い込みの原因になるので、大きい状態から小さくする向きにします）。

**9. 設計原稿との一致は、数値でしか判定しない。**
重ね合わせた画像や並べた画像を見て「ほぼ一致」と言ってはいけません。`figma_compare.ts` が出す平均差・最大差で判定します。基準は同スクリプトの説明にあります。

## テロップと音声

**10. テロップはセリフの逐語にする。**
要約してはいけません。長い文はフレーズごとに画面を分け、実測した発話の時刻に同期させます。同じテロップを2回出さないでください。

**11. 表示のタイミングは `align_captions.py` で機械的に合わせる。**
かなの文字数比などで推定して割ってはいけません。音声を差し替えた、シーンを割った、文言を変えた——このどれかをしたら、書き出しの前に必ず実行します。信頼度が低い箇所は警告が出て現状維持になるので、警告が出たらその箇所を目で確かめます。

**12. シーン境界の配線に、音声認識のトークン時刻をそのまま使わない。**
±0.7秒ずれます。無音検出で測った発話のかたまりを使って配線し、書き出したあとに境界の前後を検証し直します。

**13. セリフは「一息の短文」単位で生成する。**
音声の崩れはクリップの長さではなく、1行の長さに比例します。1チャンク＝1短文（約1.5〜2.5秒・1つの内容）にして、シーンと1対1で対応させます。複数の文を1つに詰め込まないでください。台本が先に確定していて、この決まりに反する長さになっている場合は、**割ることを提案する**か、**そのまま通してリスクを明示する**かを依頼主に選んでもらいます。黙って長いまま投げないでください。

**14. 音声の合否は「台本と一致しているか」で判定する。機械の一致だけで合格にしない。**
2つのエンジンが一致しても、両方が同じ間違いをすれば通ってしまいます。必ず台本の文字と突き合わせ、波形も確かめてください。
**数字・固有名詞・読み方の辞書に載せた語は、人の耳で確かめます**（`scripts/ear_check.py`。対象語だけを切り出して A/B/どちらも不可 を聞く）。
**「どちらも不可」が2回続いた語は、テイクを重ねません。** その語を言い換える案（同じ意味の別の語）か、その一言だけナレーション型に切り替える案を依頼主に出します。実際に「空き家」の一言で、生成のみ8テイク・参照音声つき2テイクのすべてが不可だったことがあります。
記録が無ければ `preflight.py` が不合格にします。AI がこの記録を書いてはいけません。

**15. 当て字を「発音は正しい」と見てよいのは、拗音・清濁・母音まで一致する場合だけ。**
さ と しゃ、と と ちょ、ん の有無などが違えば別の音です。不合格にしてください。迷ったら不合格側に倒します。

**16. 読み方の辞書を通してから生成する。**
| どこに書くか | 何に効くか |
|---|---|
| `scripts/pronunciation_dict.json` | キット標準。どの案件でも起きる読み違い。キットを新版に差し替えると入れ替わる |
| `ai-ad.config.json` の `pronunciationDictExtra`（既定 `knowledge/pronunciation.json`） | この案件の語（商材名・固有名詞）。新版に差し替えても残る |

効く範囲は次のとおりです。**AI 映像制作ツールに渡すプロンプト**（`generate_lines.py` は台本の置換とガイド、`generate_talking_head.py` はガイドのみ、`generate_google.py` は**置換だけ**——Google には発音を指定する公式の手段が無いため）と、**音声認識の結果の正規化**（`asr_gate.py`・検品）に効きます。
**読み上げ（`generate_tts.py`）には効きません。** 読み上げは台本の表記をそのまま読むので、読み違う語は台本側の表記を開いて（漢字をひらがなにして）ください。

辞書の `guide` には、**誤読の候補を `never 〜` で名指しで並べます**。1テイク目が誤読したら、その誤読を `guide` に足してから2テイク目を投げてください。テイク数を増やさずに直せます。複合語を読点で区切る指定は不自然な間になるので禁止です。一語として読みを書きます。

**17. 同じ語で2回失敗したら、3回目を投げない。**
先に原因（台本の書き方・辞書・割り方）を直します。かなで失敗したら漢字を、漢字で失敗したらかなを試します。それでも駄目なら、言い換える／画面の文字にする、に切り替えます。
回数は `out/takes-ledger.json` が数えていて、3回目は**スクリプトが拒否します**。`--force "理由"` で越えられますが、理由は台帳に残ります。

**18. 音声の不良は、作り直す前に無コスト救済を試す。**
頭と尾のカット、無音区間の詰め、間の詰め（`audio_tools.py cut-gaps`）、音量の調整の順に検討します。

**19. 既存の音声を切り貼りするときは、全文の音声認識だけで合格にしない。**
継ぎ目の前後1秒を細かい閾値で無音検出し、0.05〜0.4秒の孤立したかたまりは個別に確かめます（全文の認識は、台本に無い音を近い語に吸収して見逃します）。語の途中では切らないでください。切るなら同じテイク内の自然な間で切ります。速度の変更は素材の段階で1回だけかけます。

**20. 再生速度の正本は動画編集ツール（Remotion）側に置く。**
音声に二重に速度を焼き込まないでください。素材には編集（頭のトリム・間の詰め・つなぎ・尾のフェード）だけを焼き、速度はシーンの `playbackRate` で指定します。`playbackRate` は映像にかかり、音声トラックはレンダー時にピッチを保ったまま合わせられます。フレームレートを下げても速度の調整にはなりません（フレーム数が減るだけで、実時間の長さは変わりません）。

**21. 既存の動画の一部を流用するときは、映像・音声・テロップ・尺をセットでそのまま持ってくる。**
音声だけ切り出して尺を変えると、図のアニメーションの時刻と合わなくなって破綻します。文言が完全に一致する素材が無い場合は、貼り合わせて文言を再現せず、単一のテイクの素材と文言の差を依頼主に二択で確認してください。

## 素材と費用

**22. 新しく作る前に、既にあるものを探す。**
画・音・部品は `knowledge/assets.md` の台帳を先に引きます。判断の基準は**制作時の台本の意味ではなく、見る人に与える印象**（感情と状況）です。印象が合えば流用できます。新しく作ったら、台帳に印象を1行足してください。使い回す前提の映像は「セリフなし・文字なし・感情と状況だけ」で作ります。

**23. 費用のかかる生成と、書き出しの前に、承諾を得る。**
第1部5節のとおりです。確認のために mp4 を書き出さないでください。速さ・尺・見た目の確認は `preflight.py` と静止画と Studio で完結します。
承諾は `scripts/approve.py` が対話で記録します。記録が無ければ生成も書き出しも動きません（第1部0節）。

**24. 素材の置き場所を区別する。**
Git に入れるもの: コード、決まり、台帳、読み方の辞書、台本の定義、**採用したテイク**の映像と音声。書き出しの再現に必要な最小限です。
Git に入れないもの: 生成した原本（`assets/videos/takes/`）、納品した mp4（`out/`）、音声認識のモデル。大きすぎます。手元に置き、必要ならクラウドにバックアップしてください。

## 表現

**25. 事実でないことを書かない。**
誇大な表現、実在しない実績、根拠のない比較優良（「最高」「日本一」）、「必ず」「絶対」「誰でも」「確実に」のような断定は使いません。特定の事例の数字を、全員に起きることのように書かないでください。他社や公的機関と誤認させる表現も禁止です。

**26. 表記は、必要な場合に入れる。**
PR であることの表記と運営者の表記は、**必要な場合だけ**です（アフィリエイト、第三者による推奨、広告主と表示者が違うときなど）。
自社が自社の商材を出すだけの動画に、機械的に付ける必要はありません。要否は案件ごとに `knowledge/project.md` §9 で決め、
`src/brand.ts` の `BRAND` / `OPERATOR` が空なら何も描かれません。
AI 生成素材を使った旨の注記と、演出であることの注記は、該当するカットに入れます。文言と出す場所は `knowledge/project.md` で決めます。条件や注意事項の打消し表示は、**読める大きさと十分な表示時間**で出します（小さすぎる・一瞬だけの打消しは打消しとして認められません）。注記はテロップより小さく、ただし読めない大きさにはしません。注記とテロップが重ならないこと（決まり1）。

**27. 数字は一次情報で裏を取り、画面に出典を入れる。**
まとめ記事や競合の広告を根拠にしないでください。調査の時点（年月）も書きます。画面から出典が消えた瞬間、根拠のない数字になります。

**28. 業種ごとの規制は、案件ごとに一次情報で確認する。**
広告表現には、全業種に共通の法令、業種ごとの法令やガイドライン、媒体の審査ポリシー、取引先のルールの4層がかかります。どれが効くかは業種によって違うので、キットの側には書きません。案件開始時に調べ、根拠（一次情報の所在と確認日）とセットで `knowledge/project.md` に書いてください。「たぶんこうだろう」は書かないでください。**迷ったら書かない**が原則です。判断がつかない表現は、制作を止めて依頼主に確認します。

## 進め方

**29. 指摘は台帳に1行ずつ記録する。**
`FIXES.md` に、指摘・対応・検証方法・状態を書きます。完了の印を付けてよいのは依頼主だけです。同じ指摘が過去に無いかを先に照合し、再発なら原因を先に書いてください。

**30. 1箇所直したら、同じ形が他に無いか必ず探す。**
同じ表が2箇所にあった、同じ変換が2箇所にあった——といったことが実際に起きます。同じ形を全部直すまで、その作業は完了していません。

**31. 自分の解釈は誤りうる。**
指示どおりに動かした「つもり」で決まりを破ることがあります。作業中はその場で静止画を確かめ、最後は第三者の検証で締めてください（第1部4節）。自分の目だけで完結させないでください。

**32. 断定する前に確かめる。**
ツールや機能の存在、技術的な可否や制限、「できない」「存在しない」という結論——これらを言う前に、公式ドキュメントで確認してください。確認していないなら、答えの冒頭に「確認していません」と明記します。推論だけで断言しないでください。

**33. 古い方式で作られたものが混ざっていないか確かめる。**
音声・映像は「いつの方式で作ったテイクか」を確認します。方式を変える前の古いテイクが残っていたら、作り直しの候補です。

**34. 同じ人物は、参照で保つ。seed では保てない。**
1カット目を採用してから、その**無加工の原本**を一時公開（`scripts/rehost.py`）して無音の source を作り、
その source を残りのカットの参照にします（手順は `guides/production.md` 4章）。
Google の座組み（Veo／Omni）には動画を参照にする入り口が無いので、**参照画像**で揃えます
（`generate_google.py --make-reference` で1枚作り、`lines.json` の `reference_image` に書く。`guides/google-models.md`）。
同じ style と同じ seed だけでは別人になります。参照が無いまま2セリフ目以降を生成しようとすると、スクリプトが拒否します。

**35. 検証は、検証する側が自分で出力を作る。**
実行役が用意した静止画や mp4 を受け取って審査してはいけません（古い画像を見せられる事故が起きます）。
審査するエージェントは自分で `npx remotion still` を叩き、成果物の検証は書き出した mp4 だけを入力にします。
